{
  "name": "public-website",
  "homepage": "https://as-tuition.github.io/AS-Tuition-Center",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/public && cp index.html dist/index.html && cp style.css dist/style.css && cp script.js dist/script.js && cp public/logo.png dist/public/logo.png",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}